export default class Calculator{
  
 add = (param1, param2)=> param1+param2;
 reduce = (value, reduceBy) =>value - reduceBy;
 divide = (value, divisor) => value/divisor;
 multiply = (value, multiplier) =>value * multiplier;
}